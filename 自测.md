# 异步更新view
1. 没有数据时，更新情况
```
1. 创建sr dns_log空表
2. 运行程序
3. 查看创建了view
2. 等待一段时间,查看view的更新情况
where recordTimestamp >= 9999999999999; 

视图 dns_log 已使用ALTER VIEW更新
DEBUG: 视图 dns_log 更新成功
数据库对 cold 的视图更新完成
所有视图时间边界更新完成
DEBUG: 释放虚拟锁成功（调试模式）
```
2. 中途插入新数据，更新情况
```
步骤：
1. INSERT INTO dns_log_local_catalog (recordTimestamp) VALUES (UNIX_TIMESTAMP());
2. 查看view的更新情况
where recordTimestamp >= 1761557489; 

视图 dns_log 已使用ALTER VIEW更新
DEBUG: 视图 dns_log 更新成功
数据库对 cold 的视图更新完成
所有视图时间边界更新完成
DEBUG: 释放虚拟锁成功（调试模式）
```
3. 新增表，多个数据表插入数据，更新情况
```
步骤：
1. 新增表acl_log，目前有两张表dns_log和acl_log
2. 重新运行程序
2. INSERT INTO dns_log_local_catalog (recordTimestamp) VALUES (1761550000);INSERT INTO acl_log_local_catalog (recordTimestamp) VALUES (1761550000);
3. 查看view的更新情况
...
where recordTimestamp >= 1761550000; 

视图 acl_log 已使用ALTER VIEW更新
DEBUG: 视图 acl_log 更新成功


...
where recordTimestamp >= 1761550000; 

视图 dns_log 已使用ALTER VIEW更新
DEBUG: 视图 dns_log 更新成功
数据库对 cold 的视图更新完成
所有视图时间边界更新完成
DEBUG: 释放虚拟锁成功（调试模式）

```