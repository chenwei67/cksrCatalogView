# 自动化测试
## 目标
完整阅读理解当前项目的功能，帮我生成测试用例以及对应的自动化测试的脚本。要覆盖正常场景和异常场景。比如：
1. 测试正常场景下的视图创建,更新和回滚
2. 测试异常场景下的视图创建，更新和回滚，比如：视图已存在、字段类型不匹配，重命名冲突等

## 测试环境准备
1. 测试环境由config.json中配置决定，所有的待测试的sr的表将会在目录./temp/sqls/下，当前有可以执行插入表的脚本为execute_sql.sh，你可以参考或利用。