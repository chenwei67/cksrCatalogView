# 测试用例改进待办

1. 用例12加强：增加数组类型与带默认值列  [已完成]
2. 用例02a实现动态分区更新，等待分区新建完成后再插入数据，并将该用例的执行顺序调整为最末尾  [已完成]
3. 用例13增加后置清理：drop掉会阻塞回滚的表或视图，以让其他用例的前置清理能正常执行回滚这一步骤  [已完成]
4. 将测试用的脚本如compare_sql_sr_tables.sh和execute_sql.sh和query_all_tables.sh移动到tests的helpers目录下  [已完成]
5. 分析当前项目，尽量增加覆盖功能更全的测试用例，比如：init时部分视图已经创建好了、表已经加好后缀了、ck表字段已经加好了三种不同场景下的初始化。同理回滚时，部分视图已经删除了，表已经去掉后缀了，ck表已经去掉字段了三种不同场景下的回滚。建议通过先init，再手动删除视图或去掉表后缀等方式实现场景的构造。  [已完成]
   - 新增：用例17（视图部分存在再init补齐）、用例18（仅后缀表存在再init补齐）、用例19（CK补列后再次init幂等）、用例20（视图已删回滚）、用例21（去后缀回滚）
6. 加强各测试用例的断言，如有必要可以插入数据并查询以检查视图的正确性，列值是否正常，增加对异常情况的检测  [已完成]
# 转测
生成测试报告，结合readme.md生成转测文档  [已完成]

交付物路径：
- README：`README.md`
- 测试报告：`docs/test-report.md`
- 转测文档：`docs/transfer-guide.md`